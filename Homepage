<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Homepage</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #1a1a1a; /* Darker solid background */
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: #ffffff; /* Default text color */
        }

        .container {
            background-color: rgba(30, 30, 30, 0.9); /* Darker background with slight transparency */
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
            padding: 40px;
            width: 400px;
            backdrop-filter: blur(5px); /* Optional: Adds a subtle blur effect */
        }

        h1 {
            color: #ecf0f1; /* Light gray color for improved contrast */
            margin-bottom: 30px;
            font-size: 2rem;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.8);
        }

        .link-container {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .link-btn {
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
            display: block;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .link-btn:first-child {
            background: linear-gradient(135deg, #2980b9, #8e44ad);
            color: white;
        }

        .link-btn:first-child:hover {
            background: linear-gradient(135deg, #1abc9c, #731b82);
            transform: translateY(-3px);
            box-shadow: 0 7px 20px rgba(0, 0, 0, 0.4);
        }

        .link-btn:last-child {
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
        }

        .link-btn:last-child:hover {
            background: linear-gradient(135deg, #219653, #27ae60);
            transform: translateY(-3px);
            box-shadow: 0 7px 20px rgba(0, 0, 0, 0.4);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome!</h1>
        <div class="link-container">
            <a href="Table.html" class="link-btn">View List List</a>
            <a href="Resume Final.html" class="link-btn">View Resume</a>
        </div>
    </div>
</body>
</html>
